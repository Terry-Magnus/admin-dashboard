export type StatsProps = {
  icon: React.ReactNode;
  name: string;
  value: string | number;
};

export type PersonProps = {
  image: string;
  name: string;
  sub?: string;
};

export type CustomerProps = {
  name: string;
  email: string;
  image: string;
};
